import { AppLayout } from "@/components/layouts";
import Hero from "@/components/Hero";

const Index = () => {
  return (
    <AppLayout>
      <Hero />
    </AppLayout>
  );
};

export default Index;
